server:
  port: 7002
  error:
    include-message: always
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: product-db
logging:
  level:
    root: INFO
    com.psc.cloud.product: DEBUG
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG