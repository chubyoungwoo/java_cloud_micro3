server:
  port: 7003
  error:
    include-message: always
spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://123.213.1.211:3306/review-db
    username: user01
    password: user01
    hikari:
      initialization-fail-timeout: 60000
logging:
  level:
    root: INFO
    com.psc.cloud.review: DEBUG
    org.hivernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE